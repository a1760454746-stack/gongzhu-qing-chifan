name: Keep Render Alive

on:
  schedule:
    - cron: '*/10 * * * *' # 每 10 分钟执行一次
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping API
        run: |
          curl -s https://gongzhu-qing-chifan.onrender.com/api/health
          echo "Ping sent to keep server alive!"
